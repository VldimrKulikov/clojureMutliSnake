server {
  listen 80;

  server_name _;

  location / {
    root /usr/share/nginx/html;
    index index.html;
    try_files $uri /index.html;
  }

  # Дополнительные настройки для ошибок
  location = /404.html {
    root /usr/share/nginx/html;
    internal;
  }
}
